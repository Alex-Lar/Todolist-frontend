{
  "name": "todo-list",
  "version": "1.0.0",
  "main": "./src/index.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn run prettier-format && yarn run lint && yarn run serve",
    "serve": "lite-server",
    "lint": "eslint .",
    "prettier-format": "prettier --config .prettierrc 'src/**/*.js' --write",
  },
  "devDependencies": {
    "eslint": "^8.42.0",
    "eslint-config-prettier": "^8.8.0",
    "prettier": "^2.8.8"
  },
  "dependencies": {
    "lite-server": "^2.6.1"
  }
}
