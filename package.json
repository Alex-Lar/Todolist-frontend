{
  "name": "todolist-client",
  "version": "1.0.0",
  "description": "Client for Todolist application",
  "main": "./src/index.js",
  "scripts": {
    "start": "npm run prettier-format && npm run lint && npm run serve",
    "serve": "lite-server",
    "lint": "eslint .",
    "prettier-format": "prettier --config .prettierrc 'src/**/*.js' --write"
  },
  "keywords": [],
  "author": "Alexander Larin",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.42.0",
    "eslint-config-prettier": "^8.8.0",
    "prettier": "^2.8.8"
  },
  "dependencies": {
    "lite-server": "^2.6.1"
  }
}
